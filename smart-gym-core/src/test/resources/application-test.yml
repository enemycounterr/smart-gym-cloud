spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test

  data:
    redis:
      host: 127.0.0.1
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: validate
    show_sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: test-github-id
            client-secret: test-github-secret

jwt:
  secret: 9z8K8HyV8Z2E2E69dMce2K9mY1EwXwI2uI4M2zC9A1A_SECRET_KEY_FOR_TESTS
  expiration: 900000
  refresh-expiration-days: 7

